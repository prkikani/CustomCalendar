CustomCalender

CustomCalender is a swift framework that provides Custom calender view as per yor theme. It contains class which can be extended to open calender in your view.

Requirements
iOS 10.0+
Xcode 12.4+
Swift 5.0+
Installation
Swift Package Manager
You can add CustomCalender to an Xcode project by adding it as a package dependency.

In Xcode, select "File" → "Add Packages..."
Enter "https://github.com/prkikani/CustomCalendar.git"
or you can add the following dependency to your Package. swift:

package(url: "https://github.com/prkikani/CustomCalendar.git", from: "1.0.0")
